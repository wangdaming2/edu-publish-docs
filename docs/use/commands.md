# 内置指令

AstrBot 预置了一系列实用的基础指令。

## 常用指令

- `/help`: 获取指令列表和帮助信息。
- `/status`: 查看当前机器人的运行状态。
- `/reload`: 重载配置文件和插件。
- `/clear`: 清除当前会话的上下文信息。

## 权限控制

指令通常分为 **普通用户** 和 **管理员** 两个级别。您可以在 `cmd_config.json` 中配置管理员名单。
