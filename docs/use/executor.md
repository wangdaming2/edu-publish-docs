# Agent 执行器

Agent 执行器是机器人对各种任务进行分发和执行的底层引擎。

## 什么是执行器

执行器决定了机器人：
- **单步任务**: 执行一个简单的指令。
- **循环任务**: 每隔一段时间重复一个任务。
- **条件任务**: 当某个条件满足时（如检测到新邮件）执行。

## 执行器配置

在 WebUI 的 **高级设置** 中，您可以查看和调整执行器的最大并发数、超时时间和重试次数。

::: warning
过度频繁的任务执行可能会导致 AI 提供商的额度快速消耗。
:::
